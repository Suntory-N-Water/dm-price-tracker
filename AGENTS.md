- ログ、コメント、コミットメッセージは日本語で記載する。
- 明示的に求められない限り、後方互換性は維持しない。
- 変更前に、変更がどこへ影響するかを確認してから実行する。
- 代替案は、現在の対応に問題がある場合のみ提示する。
- 不明な点は推測で断言せず、Web、公式ドキュメント、GitHub issueなどを調査してから回答する。
  - 調査しても根拠が得られない場合は、「わからない」と明示する。
- 新規helper関数は原則として追加しない。
  - 次のいずれかに該当する場合のみ追加する。
    - 2箇所以上から呼ばれる。
    - 分岐、検証、副作用の境界を持ち、インラインでは明確に読みにくい。
    - 既存の同種パターンに揃える必要がある。
- 1箇所からしか呼ばれない整形、`map`、変換処理はインラインにする。
- 新規helper関数を追加した場合は、変更後に呼び出し箇所の数を確認する。
- 呼び出し箇所が1箇所だけの場合は、インライン化しない理由を確認する。
- 既存パターンを流用する前に、隣接する実装ファイルを最低1つ読み、実装を揃える。

| 目的     | コマンド                                   |
| -------- | ------------------------------------------ |
| 静的解析 | `pnpm run ai-check`（Stop hookで自動実行） |
| テスト   | `pnpm run --filter <app> test`             |

- GitHubの情報取得には`gh`コマンドを使用する。
- GitHub Actions関連ファイルを変更した場合は、`/general-dev-skills:actions-check`で静的解析を実施する。
- `lint`エラーが発生した場合は、`unsafe-fix`を使用してから個別に修正する。
