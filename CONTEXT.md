# デュエルマスターズ メルカリ価格リサーチ

デュエルマスターズのカードをメルカリから定期収集し、価格推移を閲覧できるようにするシステムです。カード情報は公式サイトから、価格情報はメルカリから収集します。

## 用語

**Product**:
公式サイトが「商品」と呼ぶ、カードの販売単位です(例: `DM26-EX2 悪感謝祭 カリスマBEST`)。ランダム封入のパック商品と、40枚固定のデッキ商品の両方を含むため「パック」「収録弾」とは呼びません。複数のCardを含みます。
_Avoid_: パック、収録弾

**Card**:
公式サイトの型番(id)に対応するカードマスターです。名前・画像を持ち、1つのProductに属します。
_Avoid_: 商品、アイテム

**SearchCondition**:
Cardに対して1件以上ひもづく、メルカリでの検索条件です。検索キーワードに加えて、除外キーワード(メルカリのexclude_keywordパラメータ、例:「まとめ」でまとめ売りを除外)を持ちます。「シークレット」「xxシークレット」のように型番からは機械的に判別できないバリアントが存在するため、1つのCardが複数のSearchConditionを持つことがあります。一覧画面はSearchCondition単位で1行表示し、価格帯が大きく異なるバリアント同士を混同しません。Cardが属するProductによるフィルタは一覧画面で必須です。
_Avoid_: 検索条件(単体では曖昧)

**枚数抽出ルール**:
出品タイトルに「n枚」等の明示的な枚数表記があれば価格をnで割って1枚あたり単価とします。表記がなければ1枚とみなします(1枚出品は枚数を明記しないことが多いため)。複数カードのまとめ売りはSearchConditionの除外キーワードでクロール時点から検索結果に含めません。

**PricePoint**:
1回のクロール実行(30分ごと)で、あるSearchConditionの検索結果1ページ目にある全出品の1枚あたり単価から算出した中央値です。推移グラフの1点として使います。
_Avoid_: 価格スナップショット、平均値

**Screenshot**:
1回のクロール実行(30分ごと)で、SearchConditionの検索結果1ページ目全体を1枚撮影したものです。同じ実行で算出されたPricePointと同じタイミングで生成され、詳細画面で価格推移と並べて表示します。R2に保存し、Object Lifecycle Ruleによる保持期間は3日間です。

**User**:
アプリの利用者(友人)です。Cloudflare Accessで認証されたメールアドレスをそのまま識別子として使います。カードの一覧・詳細は全員共通で閲覧でき、クロール対象(Card・SearchCondition)の設定権限は持ちません(管理者用画面はAccessポリシーで開発者のメールアドレスのみに制限されるため、User側に権限フラグは持たせません)。

**Watch**:
UserとSearchConditionの関心関係です。「お気に入り一覧」表示に使います。MVPでは登録のみを対象とし、価格変動時の通知送信はスコープ外です。
